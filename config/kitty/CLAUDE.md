This directory contains dotfiles configuration files for Kitty terminal.
These are symlinked to ~/.config/kitty/... with basename same as they are in this directory.
Typically, symlinking is managed via a nix configuration for kitty.
When asked to create a new file that goes in this directory, I usually want the companion symlink to also be created, but ask me to confirm.
