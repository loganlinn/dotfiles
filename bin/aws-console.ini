[acm]
_base = regional
_default = /acm/home?region=${REGION}

[cloudwatch]
_base = regional
_default = /cloudwatch/home?region=${REGION}
dashboards = /cloudwatch/home?region=${REGION}#dashboards/
log-groups = /cloudwatch/home?region=${REGION}#logsV2:log-groups
log-insights = /cloudwatch/home?region=${REGION}#logsV2:logs-insights
metrics = /cloudwatch/home?region=${REGION}#metricsV2
alarms = /cloudwatch/home?region=${REGION}#alarmsV2:

[cw]
_alias_of = cloudwatch

[controltower]
_base = regional
_default = /controltower/home?region=${REGION}

[dynamodb]
_base = regional
_default = /dynamodbv2/home?region=${REGION}

[ddb]
_alias_of = dynamodb

[ec2]
_base = regional
_default = /ec2/home?region=${REGION}
instances = /ec2/home?region=${REGION}#Instances:
load-balancers = /ec2/home?region=${REGION}#LoadBalancers:
target-groups = /ec2/home?region=${REGION}#TargetGroups:
autoscaling-groups = /ec2/home?region=${REGION}#AutoScalingGroups:
key-pairs = /ec2/home?region=${REGION}#KeyPairs:
eips = /ec2/home?region=${REGION}#Addresses:
amis = /ec2/home?region=${REGION}#Images:
launch-templates = /ec2/home?region=${REGION}#LaunchTemplates:

[ecr]
_base = regional
_default = /ecr/home?region=${REGION}

[ecs]
_base = regional
_default = /ecs/v2/clusters?region=${REGION}
clusters = /ecs/v2/clusters?region=${REGION}
task-definitions = /ecs/v2/task-definitions?region=${REGION}

[eks]
_base = regional
_default = /eks/home?region=${REGION}

[elasticache]
_base = regional
_default = /elasticache/home?region=${REGION}
redis = /elasticache/home?region=${REGION}#/redis
subnet-groups = /elasticache/home?region=${REGION}#/subnet-groups
parameter-groups = /elasticache/home?region=${REGION}#/parameter-groups

[glue]
_base = regional
_default = /glue/home?region=${REGION}

[iam]
_base = global
_default = /iam/home?region=${REGION}
roles = /iam/home?region=${REGION}#/roles
policies = /iam/home?region=${REGION}#/policies
users = /iam/home?region=${REGION}#/users
groups = /iam/home?region=${REGION}#/groups

[kafka]
_base = regional
_default = /msk/home?region=${REGION}#/landing
clusters = /msk/home?region=${REGION}#clusters
connectors = /msk/home?region=${REGION}#connectors
plugins = /msk/home?region=${REGION}#customPlugins

[msk]
_alias_of = kafka

[kms]
_base = regional
_default = /kms/home?region=${REGION}

[rds]
_base = regional
_default = /rds/home?region=${REGION}
databases = /rds/home?region=${REGION}#databases:
snapshots = /rds/home?region=${REGION}#snapshots:
parameter-groups = /rds/home?region=${REGION}#parameter-groups:

[route53]
_base = global
_default = /route53/v2/hostedzones?region=${REGION}

[dns]
_alias_of = route53

[s3]
_base = regional
_default = /s3/home?region=${REGION}

[secretsmanager]
_base = regional
_default = /secretsmanager/home?region=${REGION}

[sm]
_alias_of = secretsmanager

[sns]
_base = regional
_default = /sns/v3/home?region=${REGION}
topics = /sns/v3/home?region=${REGION}#/topics
subscriptions = /sns/v3/home?region=${REGION}#/subscriptions

[sqs]
_base = regional
_default = /sqs/v3/home?region=${REGION}

[ssm]
_base = regional
_default = /systems-manager/home?region=${REGION}
parameter-store = /systems-manager/parameters?region=${REGION}

[sso]
_base = regional
_default = /singlesignon/home?region=${REGION}
permission-sets = /singlesignon/home?region=${REGION}#/permissionSets
organization = /singlesignon/home?region=${REGION}#/instances

[vpc]
_base = regional
_default = /vpcconsole/home?region=${REGION}
vpcs = /vpcconsole/home?region=${REGION}#vpcs:
subnets = /vpcconsole/home?region=${REGION}#subnets:
