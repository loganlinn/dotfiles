{
  programs.lazygit = {
    enable = true;
    settings = {
      # autoFetch = true;
      # autoPull = true;
      # autoPullInterval = 60;
      # autoPullOnBoot = true;
      # confirmSync = true;
      # gitCommand = "git";
      # logLevel = "info";
      # openCmd = "code";
      # openCmdArgs = [];
      # proxy = "";
      # refreshStatus = true;
      # showStatusBar = true;
      # sideBarMode = "float";
      # sideBarPosition = "left";
      # sideBarWidth = 300;
      # statusBarTheme = "dark";
      # syncConflicts = true;
    };
  };
}
