---
date: 2026-03-29
git_revision: 6a928fa7
model: claude-opus-4-6
claude_version: 2.1.86
status: done
---

# Plan: Consolidate duplicated nix.settings

## Current state

`nix.settings` is defined in 6 places with overlapping and conflicting values:

| Location | Pattern | Issues |
|----------|---------|--------|
| `options.nix` (L241-261) | Canonical defaults under `config.my.nix.settings` | nix-community key may be missing trailing `=` |
| `nixos/modules/common.nix` (L138) | `nix.settings = my.nix.settings;` pass-through | Correct pattern |
| `darwin/modules/common.nix` (L86-91) | `my.nix.settings // { ... }` with darwin overrides | Correct pattern |
| `nixos/nijusan/configuration.nix` (L37-48) | Hardcoded substituters/keys | **Wrong nix-community key**, uses `substituters` not `extra-substituters` |
| `home-manager/nijusan.nix` (L166-177) | Standalone `nix.settings` | Duplicates options.nix, adds `accept-flake-config`, `run-diff-hook` |
| `home-manager/wijusan.nix` (L125-140) | Standalone `nix.settings` | Same duplication, adds `trusted-users = ["root" ...]` |

Additionally, `nixos/nijusan/cachix.nix` + `cachix/nix-community.nix` add a third layer (auto-generated by `cachix use`).

### Key conflict

| Setting | options.nix | nijusan/configuration.nix | cachix/nix-community.nix |
|---------|------------|--------------------------|--------------------------|
| nix-community key | `...CimDSUo8Zy7bkq5CX+/rkCWyvRCYg3Fs` | `...dde0946WDTTkh8+bZQITgBR7ZMEH2eyJw=` | `...CimDSUo8Zy7bkq5CX+/rkCWyvRCYg3Fs=` |

The key in `nixos/nijusan/configuration.nix` is wrong — substitutions from nix-community cache may be silently failing.

## Steps

### 1. Fix nix-community public key in options.nix

Canonical key from cachix: `nix-community.cachix.org-1:mB9FSh9qf2dCimDSUo8Zy7bkq5CX+/rkCWyvRCYg3Fs=` (with trailing `=`). Verify with `cachix use nix-community --dry-run`.

### 2. Add missing settings to options.nix defaults

Add `accept-flake-config = mkDefault true;` and `run-diff-hook = mkDefault true;` — currently only in home-manager configs.

### 3. Remove hardcoded nix.settings from nixos/nijusan/configuration.nix

Delete entire `nix.settings` block (L37-48). Settings come from `self.nixosModules.common` → `my.nix.settings`.

If nijusan needs "root" in `trusted-users`, add as host-specific override:
```nix
nix.settings.trusted-users = ["root"];
```

#### Decision

Let's keep root in trusted-users on nijusan.

### 4. Evaluate cachix.nix files

**Recommended**: Keep `cachix.nix` and imports as-is. The NixOS module system merges lists, so duplication with options.nix is harmless. Maintains compatibility with `cachix use` regeneration. Just verify key in `cachix/nix-community.nix` matches options.nix.

### 5. Replace nix.settings in home-manager/nijusan.nix

Replace standalone block with:
```nix
nix.settings = config.my.nix.settings;
```

### 6. Replace nix.settings in home-manager/wijusan.nix

Replace standalone block with:
```nix
nix.settings = config.my.nix.settings // {
  trusted-users = ["root" config.home.username];
};
```

### 7. Update nijusan CLAUDE.md

Remove `## nix settings` section and checklist item — settings now come from shared module.

## Verification

1. `grep -rn 'nix\.settings' --include='*.nix'` — only canonical locations should define settings
2. `nix eval .#nixosConfigurations.nijusan.config.nix.settings --json | jq .` — verify keys
3. `sudo nixos-rebuild dry-build --flake .#nijusan`
4. `home-manager build --flake .#nijusan` and `--flake .#wijusan`

## Risks

1. **Cache key mismatch** — the wrong key in nijusan means cache substitutions may already be failing silently. Fixing is net positive.
2. **List merge semantics** — switching from `substituters` to shared `extra-substituters` changes merge behavior. Verify with `nix eval`.
3. **HM vs NixOS nix.settings** — both configuring `my.nix.settings` is correct (system daemon vs user `~/.config/nix/nix.conf`).
4. **cachix use regeneration** — may reintroduce duplication. Harmless due to module system merging.
5. **nixUnstable vs nixVersions.stable** — different nix versions between system and user config. Separate concern, note as follow-up.
